# Core Dependencies (Required)
flask==3.0.0
flask-cors==4.0.0
sqlalchemy==2.0.23
python-dotenv==1.0.0
pydantic==2.5.0

# Testing (Required for tests)
pytest==7.4.3
pytest-asyncio==0.21.1

# Utilities (Required)
numpy==1.24.3
pandas==2.1.3
tqdm==4.66.1

# Optional: Vector Search (Recommended but not required)
# Uncomment these lines for best performance:
# faiss-cpu==1.7.4
# sentence-transformers==2.2.2
# torch==2.1.0
# transformers==4.35.0

# If you want to use vector search, install manually:
# pip install faiss-cpu sentence-transformers

# Note: The system works without FAISS/sentence-transformers
# It will use text-based search as a fallback